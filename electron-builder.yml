appId: com.simple-claude-gui.app
productName: Simple Claude GUI
directories:
  buildResources: resources
  output: dist-build
files:
  - dist/**/*
extraResources:
  - from: resources/icon.png
    to: icon.png

publish:
  provider: github
  owner: DonutsDelivery
  repo: simple-claude-gui
linux:
  target:
    - AppImage
    - deb
    - rpm
  category: Development
  icon: resources/icon.png
  executableName: simple-claude-gui
  desktop:
    StartupWMClass: simple-claude-gui
  synopsis: Multi-session Claude Code terminal manager
  description: Desktop app for managing multiple Claude Code sessions across projects

appImage:
  artifactName: Simple-Claude-GUI-${version}.${ext}

deb:
  artifactName: Simple-Claude-GUI-${version}.${ext}
  depends:
    - libnotify4
    - libxtst6
    - libnss3

rpm:
  artifactName: Simple-Claude-GUI-${version}.${ext}

mac:
  target:
    - dmg
    - zip
  category: public.app-category.developer-tools
  icon: resources/icon.png
  artifactName: Simple-Claude-GUI-${version}-${arch}.${ext}

win:
  target:
    - nsis
    - portable
    - appx
  icon: resources/icon.ico
  artifactName: Simple-Claude-GUI-${version}-${arch}.${ext}

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  artifactName: Simple-Claude-GUI-Setup-${version}.${ext}

portable:
  artifactName: Simple-Claude-GUI-Portable-${version}.${ext}

appx:
  applicationId: SimpleClaudeGUI
  displayName: Simple Claude GUI
  identityName: SimpleClaudeGUI
  publisher: CN=DonutsDelivery
  publisherDisplayName: DonutsDelivery
  languages:
    - en-US
  artifactName: Simple-Claude-GUI-Store-${version}.${ext}
