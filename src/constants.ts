// Timing constants for polling and timeouts


/** Window in ms to detect and ignore duplicate API prompts */
export const API_DUPLICATE_WINDOW_MS = 2000

/** Timeout in ms for waiting for session creation */
export const API_SESSION_TIMEOUT_MS = 30000
